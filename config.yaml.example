# Orchestratia Agent Daemon Configuration
# Copy to /etc/orchestratia/config.yaml and customize

hub_url: "https://staging.orchestratia.com"

# Authentication - use ONE of the following:
# 1. For first-time registration, get a token from the dashboard (Agents -> Register Agent)
registration_token: "orcreg_xxx"
# 2. After registration, paste the API key here and remove registration_token
api_key: ""

agent_name: "dev-linux-staging"

repos:
  pinaka-gateway:
    path: /home/ubuntu/pinaka-edge-v2-rust
    branch: staging
  pinakaedgecom:
    path: /home/ubuntu/pinakaedgecom
    branch: staging

claude:
  binary: claude
  allowed_tools: "Bash,Read,Edit,Write,Grep,Glob"
  max_turns: 50
  timeout_minutes: 30

session:
  backend: screen
  log_dir: /var/log/orchestratia
  pid_dir: /var/run/orchestratia
  reconcile_on_start: true
